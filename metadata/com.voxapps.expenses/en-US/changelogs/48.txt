- feat(expenses): the list pages where it narrows — an (archivedAt, dateTime) index under the walk, a Paging3 window for the scroll, a cold snapshot for the screens that hold a list, and a ui state that carries no rows
- refactor(expenses): the list narrows in the database — filters and sort in the query, vocabularies as column aggregates, Unicode matching as the residual
- fix(expenses): the standing notification follows every save, delete and midnight — not yesterday's total
