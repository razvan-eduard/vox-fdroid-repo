- feat(design): every logic sheet rides Commander's overlay shell — fullscreen, no handle, drag the content to dismiss; the rule editor gains an enable switch and proposals surface newest-first
- feat(notes): the format bar folds into menus and the selection menu learns the styles
- feat(notes): a note is a journal — photos and voice notes live inline, one to a row
