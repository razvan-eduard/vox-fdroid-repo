- fix(apps): an imported field that is missing reads as missing, never as the word null
- feat(applock): the lock trio, the db key and the midnight scheduler live once
- feat(i18n): one loader and one parity rule, held by a test in every app
- feat(ipc): the contract says everything once, and the manifests advertise what the code answers
- fix(calendar): a stored name this build does not know degrades to the default
