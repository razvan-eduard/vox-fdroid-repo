- feat(vision): Vox LiveView — the camera as a reader, behind its own icon
- refactor(vision): the camera's shared floor moves out of the scan screen
- feat(design): every settings-menu row says what its page holds
