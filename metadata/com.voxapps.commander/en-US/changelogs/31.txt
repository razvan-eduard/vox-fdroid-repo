- feat(commander): the GPU reports its budget, so a model too large stays on the CPU
- fix(commander): the GPU fixture is quantized, like the models it stands in for
- fix(commander): the GPU test answers offline, and asks about capacity separately
